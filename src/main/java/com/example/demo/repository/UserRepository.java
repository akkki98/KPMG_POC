package com.example.demo.repository;

// Deprecated inert repository for removed duplicate UserEntity.
// Retained only to avoid ref errors if something still imports it.
@Deprecated
public interface UserRepository { /* inert */ }
