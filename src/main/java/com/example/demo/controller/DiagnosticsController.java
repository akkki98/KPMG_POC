package com.example.demo.controller;

// Diagnostics controller disabled because messaging is turned off.
// Keeping an empty class to avoid bean creation errors.
public class DiagnosticsController {
}