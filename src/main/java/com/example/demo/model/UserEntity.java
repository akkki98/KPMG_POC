package com.example.demo.model;

import jakarta.persistence.*;
import lombok.*;

// Deprecated placeholder. Original duplicate of User (UUID) mapped to table 'users'.
// Left intentionally non-functional to avoid Hibernate mapping conflicts.
// Do NOT remove if migrations or historical references rely on the filename; instead,
// keep it inert. Remove later when safe.
@Deprecated
public class UserEntity { /* inert duplicate removed */ }
